apiVersion: v1
kind: Service
metadata:
  name: snow-terraform-agent
  labels:
    app: snow-terraform-agent
spec:
  type: ClusterIP
  selector:
    app: snow-terraform-agent
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
